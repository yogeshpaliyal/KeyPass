
name: Common Worflow

on:
  workflow_call:

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v3

      - name: 🧱 Set Up JDK
        uses: actions/setup-java@v1
        with:
          java-version: 11
          
      - name: Grant Gradlew rights
        run: chmod +x gradlew